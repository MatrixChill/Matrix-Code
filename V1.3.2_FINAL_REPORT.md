# Matrix Code v1.3.2 Final Report

## Git

- Branch: `wip/v1.3.2-night-20260908-0040`
- Base recebida: `1c2808c332`
- HEAD de implementação validado: `8e7884eeb0`
- Commits novos:
  - `cfb65eb6fb fix(tui): stabilize Windows paste handling`
  - `3cfd87efd8 fix(windows): preserve console and harden portable launcher`
  - `1d918d8c50 build(windows): make Matrix Voice packaging reproducible`
  - `8e7884eeb0 fix(windows): track OmniRoute shim lifecycle`
  - `docs: prepare v1.3.2 release report` (commit que contém este relatório)
- Status antes do commit de documentação: limpo; artefatos de build permanecem ignorados.
- Integração: `git merge-tree` contra `dev` terminou com exit 0 e sem conflitos.

## Keyboard

- Ctrl+C idle: PASS; consumido sem encerrar o TUI.
- Ctrl+C interrupt: PASS; dispara `session.interrupt` com sessão ativa.
- Ctrl+C selection: PASS; continua pertencendo ao intercept de seleção/cópia.
- Ctrl+V: PASS automatizado e PASS no teste manual anterior do usuário.
- Ctrl+Shift+V: PASS pela mesma rota de fallback de clipboard.
- Shift+Insert: coberto pela rota de fallback; confirmação física ainda manual.
- API key paste: PASS automatizado, preservando bytes e evitando duplicação.
- Secret masking: PASS; maiúsculas/símbolos preservados, conteúdo real não aparece no render.

## Windows Console

- Causa original: `Start-Process -WindowStyle Normal` em toda execução manual criava outro Console Host e aplicava outro perfil de fonte/escala/layout.
- Correção: `matrix.ps1` manual executa `matrix.exe` no console atual; caminhos ocultos/redirecionados continuam usando processo filho.
- Console Host: nenhuma chamada encontrada para alterar fonte, janela ou buffer; preservação visual física pendente de confirmação humana.
- Windows Terminal: o launch manual permanece na aba/console atual; confirmação visual física pendente.
- `matrix.ps1`: probe seguro sob StrictMode, metadata smoke sem estado, console atual no uso manual.
- `matrix.cmd`: auxiliar oculto, somente TUI visível, preferência por PowerShell 7 e fallback Windows PowerShell, sem ExecutionPolicy Bypass.
- Não existem chamadas a `SetCurrentConsoleFontEx`, `SetConsoleScreenBufferSize`, `SetConsoleWindowInfo`, `Console.SetWindowSize` ou `Console.SetBufferSize` no produto.
- `terminal-win32.ts` não foi alterado: preserva o modo inicial, controla somente `ENABLE_PROCESSED_INPUT`/raw mode e restaura o valor original.

## OmniRoute

- Version testada: `3.8.50`, instalação global via `omniroute.ps1`.
- Cold-start: PASS; porta livre, listener `node.exe ...omniroute...server-ws.mjs` identificado por PID, nome, caminho, command line e árvore de processos.
- Warm-start: PASS; segunda instância reutilizou o mesmo PID 20128 e o mesmo PID 20260, sem listeners duplicados.
- PID handling: PASS; worker 34224, supervisor 31628 e PID rastreado 31628 no teste final da lógica; ao encerrar o TUI, 20128 e 20260 ficaram livres.
- `/v1/models`: o endpoint respondeu e comprovou o serviço; a instalação testada exige autenticação para retornar o catálogo.
- Global `.ps1` shim: PASS, incluindo espera após saída do wrapper e adoção do supervisor real.
- `.exe`/`.cmd` support: validado estaticamente pelo launcher validator e Pester.

## Matrix API

- Porta 20260: PASS.
- Startup: PASS; processo identificado como `matrix.exe matrix-api` da extração limpa.
- Reuse: PASS no warm-start, sem duplicação.
- Health autenticado: HTTP 200, 2 modelos no teste.
- Shutdown: PASS; listener removido ao encerrar o TUI proprietário.

## Model Discovery

- Live/static: presente; `MatrixCatalog.fromGatewayModels()` substitui o catálogo built-in quando o gateway fornece modelos e mantém fallback para lista vazia/indisponível.
- Total models: presente em routing status e TUI.
- Status: health live/built-in e falhas observadas estão presentes.
- Git: funcionalidade já estava na base comum (`077fb97aab` e commits anteriores); não foi modificada nesta reta final.
- Testes focados: 26 PASS.

## Matrix Voice

- Build: PASS canônico, sem cópia fake ou reaproveitamento manual de executável.
- Dependências: `faster-whisper 1.2.1`, `huggingface-hub 1.29.0`, `numpy 2.5.2`, `PyInstaller 6.22.2`, `sounddevice 0.5.6`, incluindo `ctranslate2` transitivo.
- Model: `Systran/faster-whisper-base`, baixado pelo script canônico.
- Self-test: PASS, `Matrix Voice ready`.

## Portable

- Path: `packages/opencode/dist/matrix-release/Matrix-Code-Windows-x64-Portable`
- ZIP: `packages/opencode/dist/matrix-release/Matrix-Code-Windows-x64-Portable.zip`
- Contents: 286 arquivos na pasta; ZIP com 303 entradas; inclui executável, launchers, Matrix Voice, templates, README e LICENSE.
- Versão do build: `0.0.0-wip/v1.3.2-night-20260908-0040-202609081900`.
- matrix.exe: 175060480 bytes; timestamp `2026-09-08 20:01:22 +01:00`.
- matrix.exe SHA256: `6BD7599CCF7BC951005F3904538BB9CC82CFA69730301A4420C73EC076DADE74`
- matrix.ps1 SHA256: `BC6644A6E80C17A09BE0364CB74A0460747F364A747661B5A219EF51DF1FAEA9`
- matrix.cmd SHA256: `183617D2F211C133A94BE40894C56DC854B3ADFE4EB22B1546B6AD750BCE3702`
- Matrix Voice helper SHA256: `D49DC34B56657F61B437B29391536DA7D4F973FC0EDFC1447A7E1C4530233F4C`
- ZIP SHA256: `F2C63440AD2FCF58F349F37785D150602027A1077914B99BE50514176A735E45`
- Launcher SHA256: fonte/portable idênticos para `matrix.ps1` e `matrix.cmd`.
- Extração independente: `matrix.exe`, `matrix.ps1`, `matrix.cmd`, sintaxe PowerShell e Voice self-test passaram usando somente o ZIP extraído; nenhum `.matrix` foi criado pelos smokes de metadata.

## Security

- Files forbidden: 0 na pasta portable, 0 no ZIP e 0 na extração final.
- Secrets: 0 credenciais reais em arquivos de texto; referências legítimas a `OPENROUTER_API_KEY`/`OMNIROUTE_API_KEY` permanecem em scripts, README e template.
- Personal paths: 0 ocorrências de caminho pessoal no portable e na extração.
- DPAPI: primeiro run gerou apenas `.matrix/state/matrix-api.cred` na extração temporária; conteúdo protegido pelo usuário e removido após o teste.
- Logs/cache/PIDs: criados somente no estado temporário de runtime; ausentes do candidato e do ZIP.
- A varredura binária ampla produz assinaturas fortuitas/fixtures dentro do executável empacotado; a revisão de arquivos de texto e do estado de build não encontrou credencial real.

## Tests

- Keyboard/paste/secret: 23 PASS, 0 fail.
- Launcher validator: 32 PASS, 0 fail.
- Pester launcher: 45 PASS, 0 fail.
- TUI typecheck: PASS.
- `git diff --check`: PASS (somente avisos de conversão LF/CRLF do Git).
- Model discovery/routing status: 26 PASS, 0 fail.
- TUI completa: 215 PASS, 1 skip, 2 falhas paralelas preexistentes.
- Testes preexistentes isolados: 9 PASS, 1 skip, 0 fail.
- Matrix Voice self-test: PASS.
- `matrix.exe --version`: PASS.
- `matrix.ps1 --version`: PASS.
- `matrix.cmd --version`: PASS, sem bypass.
- Build canônico Windows/Voice/standard/portable/ZIP: PASS.
- Cold-start, warm-start, API 20260 e cleanup: PASS.

## Pre-existing Issues

- `DiffViewerFileTree > keeps loading and error quiet while rendering an empty settled state` falha apenas na suíte paralela e passa isoladamente.
- `providerOptions > includes a synthetic Other option for custom providers` herda locale inglês na suíte paralela e passa isoladamente.
- O teste snapshot hierárquico do DiffViewer permanece marcado como skip pela suíte existente.

## Manual Tests Still Required

- Executar o portable em um Windows Console Host real e em Windows Terminal, confirmar que fonte, escala, janela e layout não mudam ao entrar/sair do TUI.
- Confirmar fisicamente Ctrl+C em tarefa ativa/idle/seleção e Ctrl+V, Ctrl+Shift+V e Shift+Insert.
- Confirmar onboarding visível com uma OpenRouter API key real e verificar o reuse após reabrir; o valor não deve ser coletado em logs.

## Release Readiness

- Automated readiness: SIM.
- Manual readiness: PENDENTE.
- Release ready: NÃO, até o teste visual/keyboard humano final.

## Release Notes

- Changelog curto, changelog detalhado, instruções para amigo, hashes e known issues estão em `V1.3.2_RELEASE_NOTES.md`.

## Exact Commands For User

### 1. Teste manual

```powershell
$portable = Resolve-Path .\packages\opencode\dist\matrix-release\Matrix-Code-Windows-x64-Portable
& "$portable\matrix.ps1"
```

### 2. Merge em dev, somente após o teste manual passar

```powershell
git switch dev
git merge --no-ff wip/v1.3.2-night-20260908-0040 -m "chore: merge v1.3.2 release candidate"
```

### 3. Push

```powershell
git push origin dev
```

### 4. Tag v1.3.2

```powershell
git tag -a v1.3.2 -m "Matrix Code v1.3.2"
git push origin v1.3.2
```

### 5. Release

```powershell
gh release create v1.3.2 .\packages\opencode\dist\matrix-release\Matrix-Code-Windows-x64-Portable.zip --title "Matrix Code v1.3.2" --notes-file .\V1.3.2_RELEASE_NOTES.md
```
