# Matrix Code v1.3.3

## Changelog curto

- Corrige Ctrl+C no Windows para interromper a sessÃ£o sem encerrar indevidamente o TUI.
- Corrige Ctrl+V, Ctrl+Shift+V e Shift+Insert, inclusive no diÃ¡logo mascarado de API key.
- Preserva o Console Host/Windows Terminal ao iniciar manualmente por `matrix.ps1`.
- Torna o launcher mais seguro e previsÃ­vel para OmniRoute e Matrix API.
- Inclui Matrix Voice corretamente no Windows Portable por um build PyInstaller reproduzÃ­vel.
- MantÃ©m o catÃ¡logo dinÃ¢mico do OmniRoute e o status de routing jÃ¡ presentes nesta linha de versÃ£o.

## Changelog detalhado

### Keyboard e secrets

- Ctrl+C com prompt vazio e sessÃ£o ativa dispara interrupt; seleÃ§Ã£o continua pertencendo ao fluxo de cÃ³pia.
- Ctrl+V usa clipboard quando o terminal nÃ£o entrega bytes de paste e evita inserÃ§Ã£o duplicada em eventos sobrepostos.
- Ctrl+Shift+V e Shift+Insert seguem o mesmo fallback.
- O diÃ¡logo secreto preserva maiÃºsculas e sÃ­mbolos pelo `event.sequence`, mantÃ©m a mÃ¡scara e nunca copia o segredo ao cancelar.

### Launcher Windows

- A execuÃ§Ã£o manual de `matrix.ps1` mantÃ©m o `matrix.exe` no console atual, evitando um novo Console Host com outro perfil visual.
- `matrix.cmd` mantÃ©m o PowerShell auxiliar oculto e abre apenas o TUI visÃ­vel; prefere `pwsh.exe` quando disponÃ­vel e mantÃ©m `powershell.exe` como fallback, sem flags de ExecutionPolicy.
- Probes de conexÃ£o recusada funcionam sob StrictMode no Windows PowerShell e PowerShell 7.
- OmniRoute aceita executÃ¡vel, runtime Node vendorizado e shims globais `.exe`, `.ps1`, `.cmd` e `.bat`.
- O probe-first reutiliza o listener existente e evita duplicaÃ§Ã£o no warm-start.
- Para shims, o launcher aguarda o endpoint, identifica o listener pela porta e command line e rastreia o supervisor OmniRoute real para cleanup dirigido.
- Matrix API usa 20260, credencial DPAPI por usuÃ¡rio, reuse e cleanup apenas dos processos iniciados pelo launcher.

### Matrix Voice e portable

- O build cria um venv isolado, instala dependÃªncias fixadas, baixa `Systran/faster-whisper-base` e empacota `matrix-voice-helper.exe` com PyInstaller.
- Os smokes do launcher usam cÃ³pia descartÃ¡vel, impedindo que `.matrix`, credenciais, PIDs ou logs contaminem o candidato.
- O ZIP final foi auditado sem arquivos proibidos, segredos reais ou caminhos pessoais.

### Model discovery

- `MatrixCatalog.fromGatewayModels()` transforma o catÃ¡logo live do OmniRoute em candidatos roteÃ¡veis.
- O status distingue catÃ¡logo `live`/`built-in`, reporta `totalModels` e mantÃ©m health honesto.

## InstruÃ§Ãµes para um amigo

1. Extraia `Matrix-Code-Windows-x64-Portable.zip` em uma pasta gravÃ¡vel.
2. Abra PowerShell ou Windows Terminal nessa pasta.
3. Execute `& .\matrix.ps1`.
4. No primeiro uso, cole a OpenRouter API key quando solicitada. Ela Ã© armazenada somente em `.matrix\state\openrouter-api.cred`, protegida por DPAPI para o usuÃ¡rio atual.
5. Valide Ctrl+V, Ctrl+Shift+V, Shift+Insert e Ctrl+C no TUI.

## SHA256

- ZIP portable: `F2C63440AD2FCF58F349F37785D150602027A1077914B99BE50514176A735E45`
- matrix.exe: `6BD7599CCF7BC951005F3904538BB9CC82CFA69730301A4420C73EC076DADE74`
- matrix.ps1: `BC6644A6E80C17A09BE0364CB74A0460747F364A747661B5A219EF51DF1FAEA9`
- matrix.cmd: `183617D2F211C133A94BE40894C56DC854B3ADFE4EB22B1546B6AD750BCE3702`
- Matrix Voice helper: `D49DC34B56657F61B437B29391536DA7D4F973FC0EDFC1447A7E1C4530233F4C`

## Known issues

- A preservaÃ§Ã£o visual real do Console Host/Windows Terminal e o comportamento fÃ­sico de Ctrl+C/Ctrl+V ainda exigem o teste humano final.
- A suÃ­te TUI completa tem dois testes antigos sensÃ­veis a locale/estado compartilhado que falham apenas em paralelo; ambos passam quando executados isoladamente.


